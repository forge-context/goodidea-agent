# 更改履历

仅记录公开官网、固定 Demo 与通用 UI 的任务变化，最新在前。此前完整产品任务记录已在产品仓库保留。

## 2026-09-09（第三次）— 统一为 Noto Sans 字体体系，重建字号层级，加深配色并给标题加一处亮色

- **字体：整页只用一套 Noto Sans，按语言分文件自托管。** 英文 Noto Sans、简体中文 Noto Sans SC、日文 Noto Sans JP，均为带字重轴的可变字体，一次请求覆盖 400 / 500 / 600 / 700。新增 `web/src/fonts.css` 声明 `@font-face` 与文本角色（`--font-sans`、`--font-code`、`--fw-hero/heading/label/body`），并在 `html[lang]` 上把 `--sans` 与 `--mono` 一并指回本页字体——`shared/studio/tokens.css` 正是在 `html[lang]` 上定义它们，写在 `:root` 会因优先级更低而失效。等宽只保留 `--font-code` 给真正的代码；导航、眉题、步骤编号 01/02/03、Demo 计时全部改回正文字体（计时保留 `tabular-nums`）。首屏插画叠字与 Demo 内的 `--gip-hand`（原楷体→日文明朝→Georgia 回退链）一并并入同一套字。`shared/studio/tokens.css` 未修改，私有产品仍用系统字体栈。
- **字体来源与许可：** 上游为 `github.com/google/fonts` 的可变字体（`ofl/notosans`、`ofl/notosanssc`、`ofl/notosansjp`），下载到已 gitignore 的 `.fontcache/`，**不入仓库**；仓库只提交子集与许可证：`web/public/fonts/{noto-sans-latin,noto-sans-sc,noto-sans-jp,noto-sans-cjk-bits}.woff2` 与 `noto-sans-OFL.txt`、`noto-sans-sc-OFL.txt`、`noto-sans-jp-OFL.txt`（SIL OFL 1.1，与各自字体同目录取得）。未复制任何系统自带字体（苹方、微软雅黑、Hiragino 等只作为回退名出现在字体栈里）。
- **子集由本站文案生成。** 新增 `web/scripts/build-fonts.mjs` + `web/scripts/fontCopyEntry.ts`：用 esbuild 打包 `siteCopy.ts` / `paperCopy.ts` / `paperBrief.ts`，按语言遍历取出实际会渲染的字符，另加完整 ASCII 与排版可能产生的标点，再用 `pyftsubset` 裁成 woff2（保留字重轴；拉丁字体先用 `varLib.instancer` 把宽度轴钉在 100）。体积：拉丁 37.4 kB、简中 127.5 kB、日文 135.9 kB。它不在 `npm run build` 里，文案改动后手动执行（需先 `.venv/bin/pip install "fonttools[woff]"`）。
- **只加载当前语言。** 页面上没有用到的字体不会被请求；四个 HTML 入口各自 `<link rel="preload">` 自己的那一个。冷启动逐页实测：`/` → `noto-sans-latin` + `noto-sans-cjk-bits`；`/ja/` → `noto-sans-jp` + `noto-sans-cjk-bits`；`/zh-cn/` → 仅 `noto-sans-sc`。`noto-sans-cjk-bits.woff2` 只有 7.0 kB，装的是英文页与日文页需要、而它们自己的字体没有的汉字——每页页尾都有「English / 日本語 / 简体中文」，语言切换上有「中文」。这些字符不是手写清单，而是由脚本读取已生成子集的 cmap 反推出来的。`public/_headers` 增加 `/fonts/*` 的一天缓存 + 一周 stale-while-revalidate。
- **字号：新增两级流体标题，正文回到 16px。** `--fs-hero`（clamp 34→60px；英文 32→50px、日文 32→54px）、`--fs-h2`（clamp 28→38px）为流体，因为标题以窗口为度量；阅读尺寸及以下固定：`--fs-hero-lead` 17→19px、`--fs-lead` 17px、`--fs-body` 16px、`--fs-small` 14px、`--fs-label` 13px、`--fs-tag` 12px。原先按 14px 排的正文（成果清单条目、三个步骤正文、三条原则、边界说明、场景说明、下载说明、展开摘要）全部回到 16px；主按钮 15→16px，并移除中日文把按钮压回 15px 的覆盖。字重当角色用：首屏标题 700（此前 CJK 被统一压成 600，已移除该覆盖），区块标题与控件 600，标签 500，正文 400。新增 `@media (max-height: 820px)` 在矮屏把标题降到 clamp 32→48px，其余不动。手机端中日文标题 27→30px。
- **配色：底色对齐插画，文字与主色加深，新增一处亮色。** `--paper` 改为 `#faf8f2`（直接从插画外围采样，与图完全一致）；`--ink` `#2b2724`→`#202820`，`--muted` `#6a6259`→`#566052`，`--primary` `#4e6446`→`#31543a`，`--primary-dark`→`#24402b`，描边略加深。新增 `--accent: #d4e86c`，**只用于**首屏标题里「可以开工 / start building / 開発に進める形」下方那道手绘线（内联 SVG 单笔，与插画上的绿线同一种笔触），不用于背景、按钮或标签。三处职能色（研究赭 / UX 蓝灰 / 工程棕）与插画本身未改，也没有加滤镜。
- **插画叠字从三处减到一处。** 只保留绿色椭圆内的「把想法，推敲成形。」（日文「アイデアを、進める形に。」／英文 "Work the idea into a shape you can build."）；顶部「一个值得做的想法」与底部「第一版，先做到这里」的 DOM 叠字删除（`siteCopy.scene` 移除 `title`/`first`，`HeroScene.tsx` 与对应 CSS 同步删除）。图片本身未改动，不加色块、不抠图，原有线条与留白保留。剩余一句用正文字体、`--fw-label`，字号 2.4cqw（英文 2.05cqw）且不低于 14px，视觉分量低于左侧说明；≤720px 时隐藏（该宽度下椭圆仅约 80px 宽）。
- **版面与文案。** 首屏分栏由 45/55 调整为 48/52、栏间距 44→56px（未缩小插画本身）。中文 Demo 标题改为「看一个报价工具的想法，如何变得具体。」，说明段删掉与动画开头重复的一句；英文首屏说明由三行压到两行，标题改为三分句；日文围绕「開発に進める形」重写。所有改动只涉及表述，未新增任何能力主张——结尾仍完整声明这是固定案例、不连接后端、不真的生成软件。
- **顺带修掉的两处：** `.closing-panel > p`（0,1,1）一直压过 `.closing-scope`（0,1,0），使结尾那段边界说明既拿不到自己的字号也拿不到 30px 上边距——导语改用 `.closing-lead` 类名，两者各归各位；焦点环由 `rgba(78,100,70,.55)` 改为 `3px solid var(--primary)` 实色外扩，此前那圈半透明绿在纸色以外的底上看不见。Demo 内四处低于 4.5:1 的标签（`gip-corner-note`、`gip-chip-quote`、`gip-chip-stage`、`gip-proposal-evidence`）在各自色相里加深。
- **兼容性：** 未改 `shared/studio/`；未改 Demo 的内容、时间轴与交互行为；未新增运行时依赖（fonttools 仅为构建字体的本地工具，esbuild 来自 Vite 已有依赖）。`/lab/` 三个历史走查照常。`siteCopy` 新增 `heroTitleMark`，`scene` 移除 `title`/`first`。
- **验证：** `npm --prefix web run build`、`npm --prefix web test`（90 项）、`npm --prefix web run typecheck`、`python3 scripts/check-public-boundary.py` 全部通过。构建产物逐页实测（1366×768 / 1440×900 / 1920×1080 / 390×844 / 360×800 × 中英日）：无横向溢出；桌面三种尺寸下 Hero 底边与 `#demo` 顶边相等（768 / 900 / 1080），首屏仍正好一屏；每页所有承载文字的元素解析出的字体家族只有一个（Noto Sans / Noto Sans SC / Noto Sans JP）；把半透明层合成后重测对比度，三语言全页无低于 4.5:1（大字 3:1）的文字，小于 13px 的只剩 `aria-hidden` 的 ▶ 符号。Demo 按每语言 10 个时间点逐帧查看，无遮挡、无溢出纸面、无意外换行；播放→暂停（时间停住）→拖动到 50s→重播回 0 均正常。`prefers-reduced-motion: reduce` 下 `--hero-p` 为 0、箭头 `animation-name: none`、区块标题 opacity 为 1、`scroll-behavior: auto`。语言切换 href 正确，成果下载仍生成 2 099 字节的 Markdown。
- **未完成 / 未验证：** 上一条记录里「动画内文字在中等宽度下偏小」的问题依旧存在——本轮只换了字体与颜色，没有改动 `paper.css` 的舞台字号或竖屏构图的启用宽度，那仍需要你决定。字体子集是按当前文案裁的：改文案后必须重新运行 `node web/scripts/build-fonts.mjs`，否则新字符会掉回系统字体（脚本的取字入口集中在 `web/scripts/fontCopyEntry.ts`，新增可见文案来源时要一并登记）。`prefers-reduced-motion` 是用 Chrome 的媒体模拟验证的，未在打开该系统设置的真机上复核。文案未经真人用户校验。

## 2026-09-09（再次）— 字号复查：修掉页面上两处低于下限的字，动画内的偏小问题另记

按 `shared/studio/tokens.css` 自己的规则复查了整页实际渲染字号（下限 14px；12–13px 只留给全大写等宽的元数据）。逐元素量了三种语言 × 1081/1100/1366/1440/1920/390 六种宽度，动画舞台按它自身的缩放折算成屏幕像素。

- **修：中日文的成果区标签从 13px 提到 14px。** `.brief-block h3` 与 `.brief-input-label` 在英文下是 13px 全大写等宽（每个字形都是 cap height，视觉重量约等于 16px 小写，符合规则）；CJK 覆盖规则把它们换回普通黑体、去掉大写和字距，却漏掉了字号，于是中日文页面上有 8 处 13px 的 CJK 词。它们本来就在那条覆盖规则的选择器列表里，只是没被加进旁边那条 `font-size: var(--fs-small)`，现在补上——`.eyebrow` 早就是这么处理的。
- **修：首屏插画上的三处叠字加了下限。** 叠字用 `cqw` 跟着插画缩放，而两栏布局最窄时（视口 1081px）插画只有 544px，此时英文圈内句 13.1px、日文底部句 12.5px、英文底部句 13.1px 都掉到下限以下。改为 `max(var(--ink-floor), Xcqw)`：桌面与平板 `--ink-floor: 14px`，手机上插画本身只有 390px 宽，强行 14px 会把标题挤出纸面并压到人物上，因此那里降为页面绝对下限 12px（另外两句在手机上本来就隐藏）。实测 1081px 下三语言的三处叠字均 ≥14px，圈内句到椭圆的归一化角距 0.70 / 0.70 / 0.82，仍在圈内。
- **未改：整体层级。** 桌面 1440 的字号阶梯是 14 / 15 / 17 / 19 / 22 / 38 / 42 / 53，各自对应正文与小字、控件、导语、步骤标题、原句、结尾标题、区块标题、首屏标题；没有相邻两级挨得过近或跳空。首屏 53px 与区块 42px 之比 1.26，配合 45/55 的分栏读起来是清楚的主次。
- **验证：** 复查脚本在中日英三语言下扫描全部承载文字的元素，排除动画舞台后，低于 14px 的只剩三处 12px 等宽序号（01/02/03，符合元数据例外）；英文页 13px 的项全部是 `text-transform: uppercase`。`npm --prefix web run build`、`npm --prefix web test`（90 项）、`python3 scripts/check-public-boundary.py` 通过。
- **未完成 —— 需要你决定：动画内的文字在中等宽度下明显偏小。** 动画舞台按 1600 单位作图后整体缩放：窗口 1238px 时缩放 0.77，窗口 976px 时 0.61，窗口 732px 时 0.46。折算到屏幕，最小的几处标签在 1440 视口下是 9.2 / 10.4 / 10.8 / 11.6px，1024 视口下是 7.2 / 8.2 / 8.5 / 9.1px，780 视口下连「采用这个方向」这颗按钮的文字都只有 8.7px、想法原句 10.3px。竖屏构图（760 单位）只在窗口 ≤720px 时启用，所以视口约 770–1180 这一段全部落在被压缩的横构图上。这一处没有改动，因为修它要么调整 `paper.css` 里的舞台字号（可能撑破纸面文字框），要么把竖屏构图的启用宽度上调并给它设上限宽度（会改变 Demo 在中等屏幕上的呈现）——两者都超出「保留 Demo 现状」的范围。

## 2026-09-09（后续）— 首屏铺满一屏，底部加一处下滑引导

- **导航 + Hero 正好等于一个可视区域。** 导航是 `position: sticky`，仍占据正常文档空间，所以高度只需减一次：`--header-h`（桌面 72px、≤720px 时 64px）成为唯一来源，`.hero` 用 `min-height: calc(100svh - var(--header-h))`，前面留一行 `100vh` 版本兜底。用 `min-height` 而不是 `height`：矮屏、放大文字或手机堆叠时区块自己长高，不裁切。实测 1366×768 / 1440×900 / 1920×1080 下 Hero 底边分别是 768 / 900 / 1080，`#demo` 的 top 与之相等——正常首屏看不到下一节。
- **文字与插画作为一块居中。** 新增 `.hero-lead` 包住左右两栏，`flex: 1` + `align-content: center`；上下留白 8px / 34px 不对称，所以这块的视觉重心比几何中心略高。左右比例、插画尺寸与叠字位置都没动——没有为了填满高度把图放大。
- **底部下滑引导。** 「向下探索」+ 一支细箭头，水平居中，独占 Hero 最后一行（不会压到插画），可见部分距窗口底 28px，另加 `env(safe-area-inset-bottom)`。它是指向 `#demo` 的普通 `<a>`：可 Tab 聚焦、可访问名就是可见文字、跟随 Hero 一起滚走而不是钉在页面底部。颜色 `#5f6b57`，在 `#fbf8f2` 上对比度 5.32:1。箭头做一次 5px 的缓慢下移再回位，2.6s 循环，只在 `data-onscreen="true"` 时运行（IntersectionObserver 控制）；`prefers-reduced-motion` 下动画为 `none`，平滑滚动也已经由既有规则退回 `auto`。三语言文案：向下探索 / Explore below / 下へ進む。
- **锚点不再落到导航底下。** 新增 `scroll-margin-top: calc(var(--header-h) + 10px)`，作用于 `:target` 与四个区块。首屏两个按钮和下滑引导现在都停在导航下方 10px（实测 `#demo` 顶部落在 82px，标题在 183px）。
- **未改动：** 插画与叠字的 16px 同步位移、Demo 的内容 / 时间轴 / 播放触发、Demo 标题那一次淡入，全部保持原样。仍是正常文档滚动，没有吸附或滚动劫持。
- **验证：** `npm --prefix web run build`、`npm --prefix web test`（90 项）、`python3 scripts/check-public-boundary.py` 通过。浏览器实测 1366×768 / 1440×900 / 1920×1080 / 390×844 四种视口 × 中英日：文档宽度等于窗口宽度，无横向溢出；390px 下 `min-height` 解析为 780px（844−64），实际高度 846px（内容更高时自然撑开）；1280×600 矮屏同样是撑高而不是裁切。点击引导后 `location.hash` 为 `#demo`，Demo 标题完整落在导航下方；引导滚出视野后 `data-onscreen` 变 `false`、`animation-name` 变 `none`，滚回首屏后恢复。
- **未完成 / 未验证：** 手机竖屏下左右两栏堆叠后 Hero 本来就高于一屏，下滑引导落在 Hero 末尾（约两屏处）——按要求它随 Hero 走、不固定在视口，因此在手机上更像「首屏结束」的标记而不是即时可见的引导。`prefers-reduced-motion` 的两条规则（箭头动画、平滑滚动）只做了 CSS 层面确认，未在开启该系统设置的机器上实测。

## 2026-09-09 — 首屏改为一张完整插画，加三处可本地化叠字

- **右侧不再是拼出来的场景，而是一张供稿插画。** `web/public/hero/goodidea-hero-illustration.webp`（1374×1145，104 KB，自带米白底 `#faf8f2`）里已经包含两个人物、主草稿、后层纸张、便签、植物、线框、划掉的一版与绿色圈线；页面不再用 CSS/SVG 重画其中任何一样。此前那版把纸张纹理、人物图集与手绘线框拼在一起，人物只能露出半个身子、线框是规整几何、纸内内容会飘到纸外——整套 `hero-draft` / `hero-figure` / `hero-plant` / `hero-spare` / `hero-wire` / `hero-cut` 实现连同 `OutcomePreview.tsx` 一起删除。
- **图片与页面底色实测差 1/255。** 插画外围中位色 `#faf8f2`，页面 `--paper` 为 `#fbf8f2`；在 1440×900 的渲染截图上跨四条边各采样 8 点，最大差值是红通道的 251→250，没有可见矩形边界，因此不加遮罩、不加混合模式。图片带 `width`/`height` 固有尺寸并预渲染进 shell，首屏不跳动、抓取到第一个响应就能开始下载。
- **插画不带文字，文字是 DOM。** 三处叠字按素材实测坐标定位（都是相对图片的百分比）：标题在顶部绿线之上，绿线本身从 (31.3%, 25.6%) 到 (69.1%, 31.2%)，因此文字跟着转 +7°；圈内一句在椭圆中心 (70.8%, 53.8%)、半径 13.1% / 10.25%，几乎不转（+2°），换行写进文案里，因为椭圆只有中间一段是宽的；结尾一句在底部绿线之上，那条线是 -3°。三语言实测：块角到椭圆的归一化距离分别是 0.77 / 0.61 / 0.66，都在圈内。便签留白，未填字。
- **左右重量改为 45 / 55。** 标题上限从 42px 提到 53px（英文 46px），中日文标题改为「一句一行」——`heroTitle` 由字符串改成分句数组，因为 `word-break: auto-phrase` 只在 Chrome 生效且不切分中文，此前把「可以」「着手できる」从中间断开。英文仍是一整句自然换行。首屏文案改为两行说明、一个主按钮、一个轻量文字链接和一行「70 秒 · 无需注册」（秒数仍由 `PAPER_SECONDS` 派生）。
- **动效只剩两处。** 插画与叠字同属一个 `.hero-art`，一起做约 16px 的垂直位移，不旋转（旋转会露出矩形底图的边）；滚动只写一个 `--hero-p`，变换全在 CSS，不触发组件状态更新；离开可视区、手机（≤720px）与 `prefers-reduced-motion` 下完全停止。Demo 标题首次进入视野时从下方 16px 淡入一次，属性由脚本在挂载后才写上，预渲染页面与无 JS 访客不会遇到隐藏状态。Demo 本体、时间轴与播放触发未改动。
- **兼容性：** 未改 `shared/studio/`，无新增依赖，`/lab/` 三个历史走查照常。`siteCopy` 移除了只服务旧首屏面板的 `preview*` 与 `heroAudience` 键，新增 `scene`。
- **验证：** `npm --prefix web run build`、`npm --prefix web test`（90 项）、`python3 scripts/check-public-boundary.py` 全部通过。浏览器实测：1440 / 834 / 390 三种宽度 × 中英日，文档宽度等于窗口宽度（无横向溢出）；滚动 0→300→0 时 `--hero-p` 走 0→0.32→0、`.hero-art` 位移 0→5.12px→0，叠字与图片的相对偏移始终是 97px（不脱离）；390px 下滚动全程 `--hero-p` 保持 0；两个首屏操作分别指向 `#demo` 与 `#brief`，Demo 与片子照常渲染；Demo 标题从 `pending` 转 `in` 并完成一次淡入位移。
- **未完成 / 未验证：** 供稿插画是通过对话传入的 WebP（1374×1145），仓库里没有更高分辨率的原图；在 4K 桌面上它会被放大到约 1.3 倍像素。手机上只保留标题一处叠字，圈内与结尾两句隐藏——它们在该宽度下会低于页面自己的可读字号下限。`prefers-reduced-motion` 的关闭路径与手机路径是同一个媒体查询，本轮以 390px 实测覆盖，未单独在开启该系统设置的机器上复核。文案未经真人用户校验。

## 2026-09-08（后续）— 统一页面语言、Demo 提前、修好决策段的点击几何

- **整页视觉统一为「清楚的产品界面 + 有温度的纸面思考」。** 语义变量在 `web/src/styles.css` 内覆盖（未改 `shared/studio/`，私有产品消费者不受影响）：暖白底 `#fbf8f2`、石墨字 `#2b2724`、主色改为足够深的鼠尾草绿 `#4e6446`；浅绿只用于选中、提案与轻量强调；研究赭 / UX 蓝灰 / 工程棕保持职能语义，不扩大为区块色。移除 body 的紫粉光晕、`.section-heading` 的紫色说明框、深紫渐变结尾条与深色页脚。统一页面级圆角（18/14/10）、描边与阴影；`.button` 只保留主 / 次两种。
- **一个标志。** 官网现有 G 路径同时用于页头、页脚、动画工作区顶栏、GoodIdea 综合卡与片尾；删除动画内的斜体小写 `g`（`GoodIdeaMark` in `stageParts.tsx`）。界面文字统一系统无衬线，手写体只留给想法原句、批注与笔迹。
- **阅读顺序改为 首屏 → 主 Demo → 完整成果 → 协作机制与决定权 → 结尾。** `#brief` 从 Demo 之前移到之后；`how` 与 `trust` 合并为一个 `#how`（三步行为 + 三条原则 + 一句产品愿景说明）；边界说明完整版只在结尾说一次，播放器旁保留简短「概念动画 · 固定示例」。首屏路线图退出主 LP，源码保留并可在 `/lab/?view=hero` 打开（它需要的文案已移入 `IdeaMap.tsx` 自己的 COPY）。
- **首屏右侧改为 GoodIdea 自身的协作与成果预览，并且是一段有层次的定格小场景**（新增 `web/src/OutcomePreview.tsx`）：三位 Agent 标记依次落位 → 各自的贡献以一张带横线的纸片落到桌面（带轻微倾角，`steps()` 过渡做定格感）→ 三张纸片沿曲线被 GoodIdea 收拢（小圆点沿同一条曲线跑，作为节奏线索）→ G 标志亮起 →「1 项待你决定」→「你已采用 · 写入产品稿」→ 产品稿 v0.1→v0.2，「产品方向 / 概念草图 / 第一版范围 / 开发交接材料」逐条写入 → 停在完成状态。15 个节拍、约 4.2 秒，节拍间距不均匀（落位后有停顿），**只跑一次不循环**，跑完后一切内容都是静态文字；有「再看一次」。不含具体案例的标题、价格与交付明细——案例只在下方 Demo 与成果区展开，避免首屏被读成报价工具官网。`prefers-reduced-motion` 与预渲染 shell 直接给到达状态；小圆点用 `@supports (offset-path)` 兜底，不支持时只画轨迹。
- **决策卡与按钮重做。** 按钮从「挂在稿面底边的独立行」改为决策卡内部最后一项，走正常文档流，顺序为 待你决定 → 提案标题 → 简短说明 → 本轮暂不做什么 → 贡献来源 → 采用按钮；桌面留 119–139 单位底部安全间距，手机留 60+。提案文案不再以「只读」为主要价值句（「先做范围确认？」「让客户在开工前，看清交付内容、修改次数和价格。」「本轮暂不做：范围调整与自动计价，留待后续。」）。
- **模拟鼠标改为「目标 + 进度」，坐标由实际布局解析。** `Scene.cursor` 不再携带 x/y，只说去 `adopt` 还是 `client` 以及走到几成；`PaperStage` 在布局副作用里量出按钮在纸面局部坐标中的框，再经纸张自身的 translate/rotate/scale 换算到舞台。箭头尖端（24 单位精灵的 4,2）成为唯一热点：精灵按它定位、按压以它为 `transform-origin` 缩放、波纹以它为圆心。移动比对应节拍早一帧结束，「到达」帧已经停稳。
- **笔迹改为按文字实际占据的框生成**（`strikeStroke` / `underlineStroke` / `circleStroke`）：删除线此前只划掉两行原句中的一行，英文工程下划线只画到三分之二，UX 的「范围可以调整吗？」落在纸张可见边界之外——现在三处都随语言与字号一起移动，并已收进纸内。融合箭头也由融合面板与提案卡的实测框生成，不再是写死路径。
- **手机改为独立阅读编排，尺寸按最终 CSS 像素定。** 实测的换算比是 0.468（390px 屏 ÷ 760 单位舞台，再减去面板自身留白），不是舞台宽度直接给的 0.513——按前者重排后，三语言 × 11 个时刻的**每一条文字都 ≥12px**，决策与客户视角段落的正文在 15–16px（缩到桌面外围的纸作为来源痕迹可更小，已从检查中排除）。
  - 窄屏产品稿改用更高的纸（570×1000 单位，纹理改 `object-fit: cover` 不拉伸），决策与客户视角前移到 1.2 倍近景；正在被读的那张工作纸也改为 1.15 倍近景（此前 0.84，一行字只有约 12px），产品稿在团队工作期间退到画面之外，创作者与念头移到纸的下方，不再互相压住。
  - 从 GoodIdea 开始综合起一次只呈现一块（融合卡 → 决策 → 客户视角），团队纸、状态条与创作者一起让位、结尾一起回来；顶栏阶段条只给当前阶段文字、其余保留序号；客户预览的三个提问与预览卡分开到不再互相覆盖。
  - 产品稿上「最初那句话」与「改写后的方向」都改为正常文档流的整块（此前是两行各自定位，长语言换行后第二行落进第一行里）。
- **减少重复状态文字。** 协作栏每位 Agent 交出贡献后只显示贡献本身（删掉三次重复的「本轮已交出」，`work` 由三条改为两条）；工作区底部的「待决定 · 1」计数删除；融合卡的取舍改为一句（工程的完整推理已在它自己的纸上读过）；`rail.summary[4]` 改为「3 份贡献 → 1 项提案」，不与顶栏状态重复同一句。
- **播放收尾统一按帧判断。** 新增 `LAST_FRAME` / `atEnd()`：此前用原始秒判断结束，播放已停止但主按钮仍显示「播放」、读数比页面宣称的时长少 1 秒。现在结束时读数显示完整时长、主按钮显示「重播」并带 ↺、点击真的从 0 重新开始。播放器控件改用品牌绿，不再是黑底按钮配紫色进度条。
- **换行与字体。** 中日文标题取消负字距、改用 `word-break: auto-phrase` + `text-wrap: balance`，两分句的句子在自己的逗号处断开（此前出现「可 / 以」「什 / 么」「报 / 价」等断词）；日文标签不再落到等宽拉丁字体；手机上中日文标题与首句适度降级字号，优先保住不从词中间断。
- **同步。** 预渲染 shell（`scripts/prerenderShell.mjs`）改写为新结构与新文案；四个页面的 `<title>`、`description`、`og:*` 与 `public/llms.txt` 一并更新；三语言 `docs/design/lp-visual-system*.md` 追加本轮记录。`briefBrief.input` 在英文下按空格拼接（此前 `join("")` 把两行粘成 "thatmakes"）。
- **兼容性：** 未改动 `shared/studio/`，未新增运行时依赖；动画样式仍全部 `gip-` 前缀。`/lab/` 的三个历史走查（hero / story / legacy）仍可打开。
- **验证：** `npm --prefix web test` 90 项通过（新增 10 项：结束边界按帧判断且最后一帧是片尾画面、两处点击只命名控件不写坐标、按下前已到达且只在 press–release 之间按下、同一秒的鼠标状态与到达方式无关、三种笔迹分别落在自己那行的中间 / 下方 / 周围且随文字变长而变长、播放到底后读数为完整时长并可重播）。`python3 scripts/check-public-boundary.py`（81 个已追踪路径）与 `npm --prefix web run build` 通过。
- **浏览器验收（本地 Chrome 无头，非预览面板）：** 新增两套逐帧检查脚本并跑到全绿——**纸面检查**（3 语言 × 4 宽度 × 10 个时刻 = 120 帧）确认没有任何印在纸上的行掉出纸外、也没有任何两行叠在一起（把相邻行框相接排除后仍抓到并修掉：UX 的开放问题压住草图、英文结尾句掉出稿面 4–32 单位、英文「当前方向 · 已更新」压住原句、日文两行原句相叠）；**字号检查**（3 语言 × 11 个时刻）确认每条文字的最终屏幕字号。点击几何 90 项全通过 = 3 语言 × 5 宽度（1440/1280/430/390/360）× 2 次点击（采用、客户确认）× 3 个时刻（到达、按下、释放），每项检查箭头尖端在按钮内、波纹与尖端重合（偏差 0）、按钮完整在产品稿内。纸面溢出检查：3 语言 × 桌面/手机 × 5 个时刻，所有印在纸上的行都在纸内。**连续实时播放实测三次**（中文桌面、英文手机、日文桌面）：均 71.3 秒墙钟（约 1 秒起播 + 70 秒片长），结束时钟 69.92，读数「70 / 70」，主按钮显示重播，点击后从 01 秒继续播放。三语言 × 桌面/手机全页截图，文档宽度等于窗口宽度（无横向溢出）；`prefers-reduced-motion` 下无时间轴、十段可逐段读到 10/10、首屏面板直接为到达状态。
- **未完成 / 未验证：** 桌面应用的浏览器面板在本环境中处于隐藏状态，其截图返回空白，因此**所有视觉验收都是用本机 Chrome 无头模式做的**，未在可见的预览面板里用眼睛连续观看。未做屏幕阅读器与全状态对比度审计。`word-break: auto-phrase` 在 Chrome 中生效，Safari 与 Firefox 会回退到普通中日文换行（已把句长调到回退时也断在逗号，但未在这两个浏览器实测）。首屏面板的小圆点依赖 `offset-path`，不支持的浏览器只画轨迹不画圆点（已用 `@supports` 兜底，未在旧浏览器实测）。文案与 Agent 名称仍是设计出来的示例，未经真人用户校验。

## 2026-09-08 — 主 Demo 改为纸面定格动画，AI 团队协作可见

- 官网 `#demo` 换成约 70 秒的纸面定格动画，实现在新增的 `web/src/studio/paper/`：自由职业设计师的报价困扰 → 写下想法 → 研究 / UX / 工程三位 Agent 各交出一份署名贡献 → GoodIdea 综合出一项待决定 → 创作者确认后产品稿才改写（v0.1 → v0.2）→ 客户视角回答开头三个提问 → 回到 GoodIdea。原 60 秒概念动画 `web/src/studio/film/` 与旧走查文件全部保留，未删除。
- 本轮新增的表达：三位 Agent 有明确身份（研究 · Mira / UX · Luca / 工程 · Kai，角色在前、名字在后，各一种标记色，不新增头像）；两处相互回应（UX 开场引用 Mira 的发现；工程引用 UX 留下的问题、在 UX 纸上圈出该行并改提更轻的第一版，协作栏用被回应者首字母标出）；桌面右侧「AI 团队」协作栏（238 / 1348 单位，约 17.7%，每位最多一行当前工作 + 一行贡献摘要，状态只在动画真的展示该贡献时变化）；以及可见的融合过程（三份署名贡献 → 共同方向 → 待取舍 → 一项待你决定）。
- 时长由时间轴派生（`PAPER_SECONDS = Math.round(DURATION)` = 70），页面文案与预渲染 shell 都读同一个值；融合段比参考小样多约 2 秒，其余段落未加速补偿。
- LP 前后文同步到新案例：`#brief` 产出示例改由 `studio/paper/paperBrief.ts` 生成（产品方向 / 概念原型 / 第一版做什么 / 不做什么 / 验收条件 / 待验证 / 交接后的实现任务），下载的 Markdown 与页面同源；三语言的 Demo 标题、介绍、场景说明、结尾文案、`llms.txt` 与 og:description 一并改写。旧「客户变更请求」案例的可操作原型入口从主 LP 撤下，源码保留并仍可在 `/lab/?view=story`、`/lab/?view=legacy` 打开。
- 正式 LP 只提供播放 / 暂停、重播、进度条与文字版；循环开关、章节跳转与秒数读数留在 `/lab/?view=paper`（另有 `&reduced=1` 预览减少动效呈现、`&w=390` 预览手机构图）。不自动播放，静态首帧是设计师 + 报价单 + 三个提问；离开可视区或标签页转后台时暂停并原位继续，不覆盖访客主动暂停；不监听 window 键盘事件。
- 兼容性与边界：`shared/studio/` 未改动，无新增依赖；动画样式全部使用 `gip-` 类名与 `--gip-` 变量前缀，`.brand`、`.btn`、`.stage` 等通用选择器未被污染。反向也修了一处：官网全局 `footer` 规则曾伸进动画内部（150px 最小高度 + 等宽字体），动画内的 `<footer>`/`<header>`/`<aside>`/`<section>` 已改为普通元素。人物图集 SVG 滤镜 id 改用 `useId`，多实例不冲突。
- 素材放在 `web/public/paper-film/`，四张纹理转为 WebP 共 456 KB（原 PNG 7.2 MB）；人物图集用高质量有损（q92，217 KB），已按 `designer-source.md` 的双通道颜色处理核对：无棋盘底、笔尖与深色描边完整。运行时不依赖 sibling 目录或仓库根目录的未跟踪图片。动画未附带网页字体：参考小样的手写子集只覆盖它自己的中文，英日会半数回退成混排，手写角色改用系统楷体 / 明朝体栈。
- 验证：`npm --prefix web test` 80 项通过（新增 27 项：时间轴确定性与循环一致、Agent 状态只在贡献发生时变化且共 9 次、融合顺序先共同方向后取舍再结论、确认前产品稿与版本号不变、客户视角收起协作、结尾恢复署名、手机构图让位与回归；播放器的首帧、暂停冻结、可视区与后台暂停后原位继续、不覆盖主动暂停、拖动定帧、重播到片尾、不监听 window 键盘、减少动效逐段、三语言）。`npm --prefix web run build` 与 `python3 scripts/check-public-boundary.py`（60 个已追踪路径）通过；构建产物中三语言页面与预渲染 shell 均为 70 秒。
- 浏览器验收：中 / 英 / 日 × 桌面（1500×1220 模拟）与手机构图（390–470px）逐段核对开场、三位 Agent、融合、确认、改写、客户视角与结尾，并按实测框位修正了英文提案卡与按钮重叠、英文融合面板溢出页脚、日文标题换行压住正文、手机融合卡与创作者念头互相遮挡、手机确认按钮压住工作区页脚等问题。资源全部 200，无控制台报错，文档宽度等于窗口宽度（无横向溢出）。
- 未完成：预览面板在隐藏状态下 `document.hidden` 为真，动画按设计暂停，因此**未能在本环境中做连续实时播放的观察**；实时时长、帧率与循环回绕只由 12fps 确定性时钟的测试与逐帧拖动核对，未用秒表实测。未做屏幕阅读器与全状态对比度审计；文案与 Agent 名称是设计出来的示例，未经真人用户校验。

## 2026-09-08 — 首页可被 Google 收录

- 排查 `https://goodidea.jianguoding.com/` 在 Search Console 显示「已抓取 - 尚未编入索引」的原因。抓取线上响应确认：没有 `noindex`，`robots.txt` 允许全部抓取并声明 sitemap，首页 HTTP 200，`http` 正确 301 到 `https`。问题出在三处自身配置。
- 根因是根页的 canonical 指向 `/en/`，即首页自己声明不是规范网址；sitemap 也只列三个语言页而不含 `/`，页面上每一条英文内链同样指向 `/en/`。改为以 `/` 为英文规范页：`/` 自指 canonical，`/en/` 指回 `/`，`hreflang` 的 `en` 与 `x-default` 改为 `/`，sitemap 列出 `/`、`/ja/`、`/zh-cn/`，`App.tsx` 的品牌链接与语言切换的 EN 一并改指 `/`。
- 首屏 HTML 此前只有空的 `<div id="root">`，正文 0 字符、无标题无链接，全部内容依赖 React 渲染，抓取与渲染之间的窗口里等于一个空白页。新增 `web/scripts/prerenderShell.mjs`，构建时把 `siteCopy` 的同一份文案写进容器：页头、hero、各区块标题与摘要、三步说明、边界条目、页脚与内链。文案仍只有一处来源，shell 为纯标记，React 挂载后整体替换。
- 新增 `web/public/404.html`。此前任意不存在的路径都返回 200 加一份首页副本，属于软 404，会让抓取预算耗在无限重复页上；新页面带 `noindex`，样式内联，指回三个语言入口。
- 三语言部署文档 `docs/deployment/cloudflare-pages*.md` 原本写着「根路径声明 `/en/` 为 canonical」，已随之改写为以 `/` 为英文规范页，并新增两节：爬虫最先读到什么（预渲染的作用，以及构建断言失败时该修匹配而不是去掉检查），以及不存在的路径（软 404 的由来与部署后用 curl 实测状态码的方法）。
- 兼容性：未改动 `shared/studio/`，未新增依赖（构建脚本复用 Vite 已有的 esbuild 读取 `siteCopy.ts` 与 `filmTimeline.ts`，两者均无 import）。保留既有暂存与工作区改动，未提交、推送或部署。
- 验证：`npm --prefix web test` 53 项通过，生产构建、`python3 scripts/check-public-boundary.py`（60 个已追踪路径）与 `git diff --check` 通过；两个新增文件均位于 `web/` 之下，符合公开边界规则。构建产物核对：四页 canonical 与 og:url 符合预期，sitemap 含 `/` 且不含非规范的 `/en/`，首屏正文从 0 字符增加到英文 3408、日文 1490、中文 1196，各页 1 个 h1、5 个 h2、3 个 h3、16 条链接，内链只剩 `/`、`/ja/`、`/zh-cn/`。浏览器验证 React 接管后无控制台报错、h1 仍唯一、装饰图正常挂载；剥掉 script 标签模拟不执行 JS 的抓取，首屏与接管后布局一致，无隐藏元素、无横向溢出。
- 未完成：本地无法验证 Cloudflare 的未匹配路径行为，部署后需实测未知路径是否返回 404，若仍是 200 则平台侧开着 SPA fallback，需要关闭。每页都被注入了 Cloudflare Bot Fight Mode 的探测脚本，尚未确认是否对已验证爬虫放行。部署后需在 Search Console 重新提交 sitemap 并对 `/` 请求编入索引；是否收录仍取决于 Google，站点上线约十天，外链与站龄不足这一项无法靠配置解决。

## 2026-09-06 — 主 Demo 改为 60 秒概念动画

- 官网改用七幕概念动画，以同一条客户原话和同一份修改确认单贯穿困扰、流程、原型、进一步设想、第一版、coding agent 交接与未来使用场景；结束后停在开放的邀请。减少细碎模拟操作，只保留三句简短逐字台词，用聚拢、宽屏原型收成客户视图、范围保留、交接光线与确认回写表达变化。
- 新增 `web/src/studio/film/`，动画与独立原型分开。播放、暂停、进度拖动、重播、减少动效逐幕阅读及文字脚本均可用；打开原型会暂停，关闭后保留编辑，重播与跳转不会污染原型状态。静态产出与下载 Markdown 继续读取同一份 `storyCopy`，交接、实现及最终使用情境保持概念示例标识。
- 旧的详细产品走查保留到 `/lab/?view=story`，交易案例仍在 `/lab/?view=legacy`。中英日 LP 入口、时长与介绍同步；设计文档和 `llms.txt` 更新，旧设计说明标记为履历。
- 修复 CJK 长标题不换行而伸进首屏地图的问题；动画网格允许缩小到手机宽度，标题、主视觉和字幕分别排版；补足平板英文地图的纵向空间，并让极窄屏页脚地址正常换行。
- 验证：53 项行为测试通过，覆盖单一帧时钟、暂停冻结、后台时间排除、跳转确认因果、探索隔离和动态减少动效设置；生产构建、`git diff --check` 与公开边界检查通过（60 个已追踪路径，另检查 5 个未追踪新文件）。Chrome 中文桌面、英文及日文 390px 手机各完整播放一遍，实测约 60.04–60.06 秒，窗口高度恒定、无滚动漂移、无页面异常；320/390/768/1024/1440px 三语言逐幕排版检查，下载内容与生成函数一致，客户确认与修改后重新确认流程通过。
- 兼容性：没有新增依赖或更改 `shared/studio/`，保留既有暂存与工作区修改；新增文件尚未暂存，未提交、推送或部署。
- 未完成：真实访客的理解、阅读节奏与购买意愿尚未测试；未做完整屏幕阅读器审计。60 秒是本地动画叙事的时长，不表示真实生成或实现耗时。

## 2026-09-06 — 完整故事 Demo 收尾修复

- 修复概念原型的边界：客户页始终只显示标记为需要客户确认的项目，内部备注不会外露；精简展示只负责隐藏原话和时间。修改已确认项目的文字会使其回到待确认状态，避免新内容沿用旧确认。
- 收紧故事结尾：用户明确说出“开始做并交给 coding agent”；交接包与 coding agent 任务分别成为独立画面；最终用同一张确认单展示手机确认和项目状态回写，再进入留白。
- 手机端交接与成果画面改为独立全屏镜头，任务列表和确认结果不再藏在同一滚动区域；三语言播放仍保持固定窗口、无横向溢出。
- 兼容性：`shared/studio/` 未改动，未新增依赖；保留既有暂存与工作区改动，未提交、推送或部署。`CHANGELOG` 记录此次修复，供后续合并前审阅。
- 验证：`npm --prefix web test` 47 项通过（新增客户可见范围、编辑后重新确认、交接决策与确认回写测试）；生产构建、公开边界检查（60 个文件）、`git diff --check` 通过。Chrome 桌面/390px 手机三语言抽查及中文完整自动播放通过：中文约 116.6 秒、英文约 118.0 秒、日文约 115.0 秒；关键交接与成果画面可见，页面滚动漂移为 0，无控制台异常或横向溢出。
- 未完成：尚未做真人用户理解、屏幕阅读器与全状态对比度审计；三语言均完成分段/逐章检查，只有中文做了完整实时播放。宣传效果和购买意愿仍需真实访客验证。

## 2026-09-06 — 完整故事 Demo 独立验收

- 无式样变更；仅评审 Claude 的完整故事实现，保留已有工作区与暂存区改动，未修改功能、提交或部署。
- 独立运行 44 项测试、生产构建、公开边界检查（60 个文件）及工作区/暂存区差异格式检查，均通过；`shared/studio/` 无改动。浏览器验证中文 18 幕桌面和手机关键画面、原型操作及交接包实际下载；下载内容与生成函数一致。
- 待修：客户页默认仍包含未勾选需要确认的第四项及内部备注；已确认项改写后仍保留确认状态。手机正常播放交接任务时，可视区约 262px，任务区域位于其顶端以下约 554px，自动播放不展示该区域；无横向溢出并不代表关键内容均可见。
- 叙事建议：补上用户明确决定第一版及交接的镜头，进一步将后半段文字卡片转为可见的成果变化。当前已具备完整流程骨架，宣传表现与真人理解效果仍待打磨；本轮未重复全程实时播放或完整三语言可访问性审计。

## 2026-09-06 — 官网 Demo 换成完整产品故事

- 官网只讲一个案例：一个人接单的设计师，想把客户散在聊天里的修改意见理清楚。原来的交易案例从官网撤下，连同它的手动走查一起移到 `/lab/?view=legacy` 作为独立实验入口，页面与地图不再混用两个故事。
- 新 Demo 是七章十八幕：说出困扰 → 想法成形 → 看见原型 → 想用起来 → 定下第一版 → 交出去 → 做成之后。每一次值得的对话都兑现一个看得见的变化，这个变化同时写进对话记录，手机上画面收进面板时仍然读得到。
- 画面不再固定在三栏里：前两章是对话加想法地图，原型一出现就占据主画面、对话退到一列，最后一屏只剩一个问题和四个真实入口（重播 / 回看成果 / 看产出示例 / 看源码）。三种布局共用一个高度，播放期间窗口不改变大小。
- 原型是可以真正操作的一条核心路径：粘贴客户留言 → 本地按换行和句号切分成修改项（页面写明不调用模型）→ 逐条校对、标记是否需要客户确认 → 发给客户 → 客户逐项确认 → 回到设计师视图看状态。每一条修改项都留着客户原话和时间，点开可追溯。
- 「继续探索 / 确定第一版 / 暂时搁置」三条方向都可点：主线走确定第一版，另外两条给出接下来要验证的问题或会保留下来的成果，并能原样返回当前画面。
- 交接包与第一版定义来自同一份数据：屏幕上的范围、不做的事、验收条件、待验证与实现任务，和「下载这份交接包（Markdown）」的内容一致，每条都带「来自：……」的出处。
- 观看与探索是两份互不写入的状态：动原型或点方向卡会暂停播放并进入探索，探索里的输入和改动不会进入固定演示，重播或跳章也不会清掉它们。回到播放停在原处，由播放按钮继续。
- 时长按脚本推导：中文约 105 秒、英文约 110 秒、日文约 105 秒（草案 90 秒，因为多了原型与第一版两屏需要阅读时间）。页面上的时长仍由 `{seconds}` 占位符填入。
- LP 同步改写：首屏、产出示例、工作方式、你保留的控制与页尾都讲同一个案例和完整价值；静态产出示例改为产品方向、概念原型、第一版做什么/不做什么、验收条件、待验证与交接后的实现任务；删掉了「查过的信息」这类此次流程里并不发生的说法。三语言标题、描述、结构化数据与 `llms.txt` 一并更新。
- 未实装的部分都写明：原型是概念预览、交接与实现任务是示例内容、页面不连后端、不会真的生成软件、也不会真的把材料发给任何 coding agent。结尾没有购买、注册、候补或伪装成可用的输入框。
- 兼容性：`shared/studio/` 未改动，共用 UI 对私有产品保持原样，无新增依赖。新增 `web/src/studio/story/`（脚本、文案、地图、原型、面板、组件、交接包）；`ideaStudioFlow.ts`、`ideaStudioCopy.ts`、原 `IdeaStudioDemo.tsx` 与 `demoScript.ts` 移到 `web/src/lab/legacy/`；删除 `studio/handoffBrief.ts`。
- 验证：`npm --prefix web test` 44 项通过（播放四拍、暂停/继续、后台暂停、跳转/重播、减少动效走完全程、探索与播放隔离、原型核心路径、方向反馈与返回、交接内容三处一致、切分器）；已用反向改动确认其中三项确实会失败。`npm --prefix web run build`、`npx tsc -b`、`python3 scripts/check-public-boundary.py` 通过。
- 控制条在这次改成固定两行：七个章节加一条长度会变的状态，在三种语言的各种宽度下要么换行（播放中把 Demo 顶高一行），要么最后一章滚出视野失去跳转入口。固定两行多占一条高度，换来位置不动、章节不藏。手机上章节改为单行横向滚动。
- 浏览器（Chrome）验收：1440×900 与 390×844 × 中/英/日，七章逐章检查——Demo 窗口高度全程恒定（桌面 826px；手机中文 704px、英文与日文 698px，含控制条仍在 844px 屏幕内），无横向溢出、无面板裁切、无控制台错误、无对外请求；实测播放期间页面滚动零漂移（桌面中文与手机日文各测一次）。中文桌面完整实时看完一遍共 108.2 秒（推导值 106.4 秒，差值来自浏览器计时器开销），结尾落在「你一直想做出来的，是什么？」；交接包下载可点且内容与画面一致。减少动效在真实浏览器实测：不自动推进，可逐步走完十八幕到结尾问题，并从「从第一步重看」回到第一步。手机上「想法地图」与「对话」两个面板都能打开。
- 未完成：台词与原型内容是设计出来的示例，未经真人用户校验；阅读停顿仍按屏幕文字长度推导，没有用真人阅读速度标定；英文与日文未各跑一遍完整实时计时。未做完整屏幕阅读器与全状态对比度审计，也没有任何转化率或购买意愿的实测数据。本次未提交、未推送，也未触发官网部署。

## 2026-09-06 — 完整流程 Demo 实现交接

- 用户决定由 Claude 负责实现；停止本次实装，并撤回刚新增、尚未接入页面的故事脚本文件。无运行式样或共用接口变更，保留此前工作区与暂存区改动。
- 将已讨论的案例、期待感累积、原型体验、MVP 决策、抽象交接和结尾留白整理为实现任务说明；最终分镜与功能尚待实装验收。本次未提交或部署。

## 2026-09-06 — 完整流程 Demo 的宣传片参考讨论

- 无式样变更；参考用户提供的 OpenAI Astra 宣传片，抽查开场、中段软件操作与结尾画面，讨论如何将「对话启发 → 产品原型 → 第一版定义 → coding agent 交接」表现为可理解的成果变化。
- 设计建议：保留一个可辨认的核心成果贯穿镜头；在原型核心操作上留出阅读时间，交接后简化实现过程，并以同一成果的使用情境收尾，再为其他产品可能性留白。尚未确定完整分镜或修改 Demo。
- 兼容性：运行代码与共用接口未改；本次不提交或部署。验证为视频关键画面抽查，未完成全片连续视听分析、用户理解测试或新流程实装验收。

## 2026-09-06 — 逐字输入演示独立复核

- 无式样变更；复核逐字输入与模式隔离实现，未修改功能代码、重新暂存、提交或发布。启动本地 5173 预览服务供查看。
- 独立重跑 40 项测试、生产构建、公开边界检查（52 个文件）及工作区、暂存区差异格式检查，均通过；确认 `shared/studio/` 无改动。
- 浏览器完成中文桌面完整播放及输入中暂停/继续检查：暂停文本保持不动，输入框高度约 107.8px 且全程一致，无输入裁切、页面滚动漂移或页面运行异常。确认手动节点编辑不再进入观看演示、返回手动时仍保留；减少动效结束后可从第一步重看且保持暂停。三语言 390px 手机页面冒烟检查无横向溢出。
- 待完善：当前台词先提出模拟执行，下一轮又用「等一下」提出相同边界，范围收缩的叙事仍可理顺；本次未改文案，未完成三语言手机全程播放、完整可访问性审计或真人阅读节奏验证。

## 2026-09-06 — 自动演示的真实台词与逐字输入

- 演示台词改写为一个真实用户的说法：把 TradingView 里已经写好的规则、上周二开盘在开会错过执行这样的具体经历说出来，并保留短回答（「不用更好的策略。把我定好的规则执行掉就行。」）。新增一幕犹豫与范围收缩：「等一下。真让它下单我还是不放心，第一版先只跑模拟吧。」——这一幕会改动地图，把「执行前先讲清楚要做什么」排到第一位，「不接管真实资金下单」这条产出示例里的边界从此可以在对话里被追溯到。中文、英文、日文各自按本语言重写，不是逐句翻译。
- 自动演示改为四拍一轮：用户发言先在演示输入框里逐字出现，停一下，再模拟发送成为对话消息，随后才出现回复与地图变化。只有用户发言逐字出现，助手回复整段落地。文本按用户可见字符切分（`Intl.Segmenter`，回退到码点），中文、日文与 emoji 不会被拆断。速度与停顿集中在 `web/src/studio/typing.ts` 按语言配置。
- 四拍纳入原有播放状态机（`autoDemo.ts`），仍然只有一个由 effect 持有的计时器：暂停会冻结半句话停在原字符，继续从同一字符往下；切后台停住、回来接着走；跳转章节、重播、切换模式都从新状态重新装计时器，上一幕不会继续输入、重复发送或补上过期回复。跳到某一章会得到完整的前置对话，地图停在上一次回复留下的状态。
- 观看模式的输入区是只读演示界面：虚线框、状态标签（只读演示 / 模拟输入中 / 准备发送）、不可聚焦，没有 textarea 或按钮。输入框从第一帧起就按脚本里最长的一句预留高度，整段播放期间高度不变，上方对话不会被顶动；手机上同样三行，盒子更紧凑。自动演示的草稿不进入手动输入框，手动输入也不进入演示。
- 修复：手动编辑过的节点文本会进入固定观看演示——观看模式现在只用脚本自己的内容，回到手动模式仍保留用户的改写。
- 修复：减少动效模式走到最后一步时下一步禁用且没有重播入口——结束时主按钮变为「从第一步重看」，回到第一步并保持手动推进。
- 演示时长按脚本与播放配置重算：中文 50.0 秒、英文 50.7 秒、日文 54.8 秒（此前约 38–40 秒，未含输入过程）。页面上「40 秒」一类说法改为 `{seconds}` 占位符，由脚本时长填入（显示为 50 / 50 / 55 秒），文案与实际不会再各说各话。
- 手动体验同步新增一步「收缩范围」：`sh`（沿用同一组台词），因此手动路径也会经过这次犹豫。
- 兼容性：`shared/studio/` 未改动，共用 UI 对私有产品保持原样，无新增依赖。新增 `web/src/studio/typing.ts`。
- 验证：`npm --prefix web test` 40 项通过（新增逐字输入、暂停、跳转/重播/切换模式后无残留动作、手动与演示互不污染、减少动效可重看等行为测试；已用反向改动确认两处修复的测试确实会失败）。`npm --prefix web run build`、`python3 scripts/check-public-boundary.py`（52 个文件）、`git diff --check` 通过。浏览器（Chrome，本地 dev）检查 1440×900 与 390×844 × 中/英/日：无横向溢出，输入框高度全程恒定（桌面 106–109px、手机 123–126px），输入文本不溢出、不裁切，四个章节跳转都得到完整前置对话，播放期间页面滚动零漂移，无控制台错误、无对外请求；减少动效模式实测不会自动推进、可逐步走到结束并从「从第一步重看」回到第一步。中文桌面完整实时观看一遍（九幕、52.0 秒），逐字输入、暂停冻结半句、继续从原字符、章节跳转与重播均按预期；手机上中文、日文播放与暂停同样确认。
- 未完成：台词是设计出来的示例，尚未经过真人用户校验；阅读停顿仍按屏幕文字长度推导，没有用真人阅读速度标定。未做完整屏幕阅读器与全状态对比度审计。桌面全程实测 52.0 秒（推导值 50.0 秒，差值来自浏览器计时器开销），文案写作「约 50 秒」；英文与日文只做了分段实测，未各跑一遍完整计时。本次未提交、未推送，也未触发官网部署。

## 2026-09-06 — LP 改版独立验收

- 无式样变更；复核待提交的 LP 与自动演示实现，未修改运行代码、提交或发布，也未重新暂存已有改动。
- 浏览器复现两处待修问题：手动编辑的节点文本会进入固定观看演示；减少动效模式到达最后一步后缺少重播入口。后续应补充对应行为测试。
- 页面定位与静态产出示例更明确；390×844 中文页面首屏区块约 1315px、产出示例约 2041px，手机阅读密度仍有压缩空间。此项为体验建议，尚未进行真实用户理解测试。
- 兼容性：确认 `shared/studio/` 无改动。独立重跑 26 项测试、生产构建、公开边界检查（51 个文件）、工作区及暂存区差异格式检查，均通过；浏览器检查覆盖中文桌面和手机、手动节点编辑及减少动效结束状态，不替代全语言与完整可访问性审计。

## 2026-09-06 — LP 改版与「观看 / 自己走」演示

- 重排全页顺序：用途与受众 → 不用操作也能读的产出示例 → Demo → 工作方式 → 你保留的控制 → 页尾再次进入体验。首屏标题改为直述用途，「想法不是直线」保留为次要品牌短句；原深色原则面板换成页尾的行动区块。
- 新增产出示例区块：一句最初的想法，加上目标用户、要解决的问题、查过的信息、第一版做什么与不做什么、验收条件、待验证假设。内容由 `web/src/studio/handoffBrief.ts` 从 Demo 自己的文案组装，页面与 Demo 不会各说各话；并明确标注这是产品定义与交接材料，不是已生成的软件，也不代表市场已被验证。
- 交易案例保留但加了场景说明：展示的是把想法整理成产品定义的过程，GoodIdea 本身不下单、不推荐策略。
- Demo 增加自动演示：默认停在可读的第一轮并给出「播放演示 / 自己试试」，不自动开始。四个阶段共八幕，中文 37.7 秒、英文 39.2 秒、日文 40.3 秒（由屏幕文字长度推导）。支持播放、暂停、继续、重播和阶段跳转；只有一个计时器由 effect 持有，暂停会同时停住对话与地图，页面切到后台时保持不动、回来继续。
- 自动演示与手动体验是两份互不写入的状态：切换不覆盖访客已做的选择，「重播演示」只重置演示，手动体验由自己的按钮重新开始；观看期间地图为只读。减少动效时不使用计时器，同样八幕改为上一步 / 下一步加阶段跳转。
- 首屏地图改为由布局生成路线：桌面三条被拒绝的捷径、手机一条，整段在 5 秒内跑完并提供重播控件。手机路线改为两列、从上往下，保留每个节点的解释句（此前手机隐藏了这些解释）。
- 修复拆仓遗留：移除指向已移出的 agent-capabilities 文档的失效链接，改为公开仓库入口；页尾与说明统一为「公开 Demo 与共用界面实现」，并写明真实工作台尚未开放、页面不连接后端。同步更新三语言标题、描述、结构化数据与 `llms.txt`，新增站点 favicon。
- 兼容性：`shared/studio/` 未改动，共用 UI 接口对私有产品保持原样，私有仓库本次无需同步。新增依赖仅为开发期测试用（vitest、jsdom、@testing-library/react），不进入生产构建。
- 验证：`npm --prefix web test` 26 项通过（演示状态机、场景脚本、计时器与模式切换的行为测试）；`npm --prefix web run build`、`python3 scripts/check-public-boundary.py`（51 个文件）、`git diff --check` 通过。真实浏览器（Chrome）检查 1440 / 1024 / 390 三种宽度 × 三种语言：无横向溢出、无控制台错误、无对外请求；播放/暂停/继续/重播/阶段跳转、模式来回切换、键盘可达与可见焦点、减少动效、后台标签页暂停均验证通过；播放过程中页面滚动位置零漂移。
- 未完成：未做完整屏幕阅读器与全状态对比度审计，也未做真实用户理解测试；本次未推送远端、未触发官网部署。演示时长与阅读节奏按文本长度推导，尚未用真人阅读速度校准。

## 2026-09-06 — LP 改版与演示模式任务说明

- 无式样变更；将 LP 评审和已确认的演示方向整理为供后续实现使用的长任务说明。
- 范围包括定位与文案、静态产出预览、点击播放的自动演示、独立的手动体验、手机阅读顺序、三语言一致性和公开信息修复。
- 明确本地验收、任务履历和发布边界；本次未实现上述功能，也未修改或发布页面。

## 2026-09-06 — LP 首次访问体验评审

- 无式样变更；评审线上中文页从首屏到页尾的产品理解、受众文案、信息层级和手机阅读顺序。
- 结论：应优先明确目标用户与最终产出，提供不依赖 Demo 操作的结果示例，将详细工程机制下沉，并在页尾保留主要体验入口。
- 发现拆仓遗留引用：英文、中文能力地图链接实测 404；日文请求遇到 429，但源码同样指向已移出的路径。页尾「公开产品实现」也需与 Demo 源码公开范围同步。本次仅记录，尚未修复。
- 验证：桌面 1440×900、手机 390×844 线上截图与 DOM 测量；手机无横向溢出，正文主要为 16–17px，主按钮为 48px 高。公开边界检查与生产构建通过。
- 未完成：以上实装建议、完整可访问性审计与真实用户理解测试。评审不等同于已测得的转化率结果；页面未修改或发布。

## 2026-09-06 — 公开仓库边界整理

- 公开目录收敛为官网、固定 Demo、通用 UI 和公开说明；此前的后端代码仍可存在于 Git 历史中，本次没有改写历史。
- Demo 沿用原有视觉与交互，共用地图、展示类型和样式保留在 `shared/studio/`，公开构建不依赖任何私有服务。
- 更新三语言 README、工作约定与 CI，加入公开路径白名单检查。Cloudflare Pages 构建入口仍为 `web`，输出仍为 `dist`。
- 验证：43 个公开文件通过边界检查；检查器拒绝私有目录、环境文件及构建依赖。生产构建、文档链接、三语言浏览器冒烟、Demo 推进、节点详情和手机布局通过，无浏览器异常或真实 API 请求。
- Git 差异检查通过。后续开发分别在公开与私有工作目录进行；本仓库不保存内部运行数据。
